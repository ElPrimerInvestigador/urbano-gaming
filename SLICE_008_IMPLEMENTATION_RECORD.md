# Slice 008 — Segment / Turn Grouping: Implementation and Validation Record

**Status: implementation candidate ACCEPTED by founder review at the local/database-backed validation level. Production validation has NOT happened — no migration has been applied to production, and this candidate is not yet applied to production or pushed.** These are two distinct decisions, not one: acceptance of the implementation as correct and locally proven is separate from, and does not imply, production deployment authorization. No `History/Slices/Slice_008/` constitutional ceremony, per the deferral already established starting at Slice 003 (see `HANDOFF.md`, `PROJECT_STATUS.md`).

## Objective (as accepted through three founder-directed design-review rounds)

Introduce a real `Segment` object grouping one or more Interaction Instances under one stable, member-facing Turn identity — proven by the Best Joke case (Open Response, then Voting, same Turn) — without touching Slice 009/010 scope (PARTICIPANTS Voting, Voting scoring), Prediction/Golazo, auth, geolocation, Leaderboards/Rewards backends, Level 33, Shared Game State, Teams, or new Engines.

The design went through three review passes before implementation was authorized: (1) initial data-model/lifecycle/migration proposal; (2) relational-integrity and ordering/concurrency pressure test, which surfaced the parent Session-row lock in `start_session_atomically` as the actual per-session serialization mechanism (traced from the real SQL, not inferred); (3) a final ordering re-decision — the founder overturned the initial `created_at`-only ordering conclusion in favor of an explicit, atomically-allocated `segment_ordinal`, on the grounds that Segment number is the canonical member-facing Turn identity and therefore warrants a stronger representation than Interaction Instance ever needed.

## Design Decisions Incorporated

- **`segments(segment_id, session_id, segment_ordinal, created_at)`** — a real table, not a bare tag column. `segment_ordinal` is the canonical Turn identity: durable, atomically allocated, never derived from `created_at` or row count. `created_at` is audit/history only.
- **No Segment lifecycle/state column.** ACTIVE / APPENDABLE / SUPERSEDED / STRANDED are derived conceptual conditions only (from the current Segment's latest Interaction Instance state, whether a newer Segment exists, and the Session's own state) — never persisted.
- **Composite relational integrity**: `segments` carries `UNIQUE (session_id, segment_id)` and `UNIQUE (session_id, segment_ordinal)`; `interaction_instances` retains both `session_id` and a new `segment_id`, enforced by a composite `FOREIGN KEY (session_id, segment_id) REFERENCES segments (session_id, segment_id)` — making cross-session Segment membership structurally impossible, mirroring the Slice 007 Candidate/Interaction fix.
- **Ordinal allocation is safe because it reuses an already-proven lock, not because of anything new.** `start_session_atomically`'s first statement (`SELECT ... FROM sessions ... FOR UPDATE`, unchanged since 0033) locks the parent Session row for the function's entire duration — traced directly from the SQL and confirmed empirically this session (see Concurrency Evidence). `NEW_SEGMENT`'s `COALESCE(MAX(segment_ordinal), 0) + 1` executes strictly inside that lock. No advisory lock, no counter table, no `Session.current_segment` pointer.
- **`SegmentTarget = "NEW_SEGMENT" | "CURRENT_SEGMENT"`**, optional, defaulting to `NEW_SEGMENT` — every pre-Slice-008 caller (including a stale cached client that never sends the field) reproduces its exact prior behavior. `CURRENT_SEGMENT` creates no new Segment; it attaches a new Interaction Instance to the session's existing current Segment, subject to the same `PreviousInteractionNotRevealedError` precondition as `NEW_SEGMENT`, plus a new `NoCurrentSegmentToContinueError` when no Segment exists yet.
- **`interactionNumber` is unchanged** — still a count of Interaction Instances, still powers the existing per-interaction cache-invalidation logic in `host.html`/`participant.html`. **`segmentNumber` is new and separate** — the current Segment's `segment_ordinal`, matched by `segmentId`, not derived from array position.
- **Minimal UI footprint**: the existing "Start Voting on the previous Turn's responses instead" button now sends `segmentTarget: "CURRENT_SEGMENT"` and is relabeled "Continue this Turn with Voting on those responses" (the old label became actively misleading once the Turn number stopped incrementing). No new buttons. No Turn Type selector.

## Data Model

```
segments(segment_id, session_id, segment_ordinal, created_at)
interaction_instances(..., segment_id)   -- new column, composite FK to segments
```

## Migration Evidence

1. `0035_create_segments.sql` — new table, `UNIQUE (session_id, segment_id)` and `UNIQUE (session_id, segment_ordinal)` at creation.
2. `0036_add_segment_id_to_interaction_instances.sql` — nullable column, PL/pgSQL backfill loop (one new Segment per existing Interaction Instance, per-session `row_number() over (order by created_at, interaction_instance_id)` ordinal, correlated by primary key not timestamp), `NOT NULL`, composite FK.
3. `0037_start_session_atomically_accepts_segment_target.sql` — 7-arg → 8-arg drop/create (established pattern), adds `p_segment_target`, resolves the Segment inside the existing session-row-locked section, returns `segment_ordinal` alongside the existing four columns.

**One real defect found during real-Postgres inspection, fixed in place**: `0036` as originally written omitted an index backing the new composite FK's referencing columns — PostgreSQL does not auto-index a foreign key's referencing side (only `segments`' own referenced side got one, via its `UNIQUE` constraints). Without it, any check of "does any Interaction Instance still reference this Segment" — which Postgres performs whenever a `segments` row is touched, including as part of a session's cascade delete — would require a full sequential scan of `interaction_instances`. Added `create index if not exists interaction_instances_segment_id_idx on interaction_instances (segment_id);`. A second, minor defect (an unused `next_ordinal` PL/pgSQL variable, dead from an earlier draft) was removed at the same time. Both fixes were verified by a full clean rollback-and-reapply of the corrected migration sequence against the same rehearsal database (see below), not merely re-read.

## Segment Targeting Implementation

`lib/session/types.ts` (`SegmentTarget`, `NoCurrentSegmentToContinueError`, `segmentNumber` on `StartSessionResult`/`GetSessionResult`) → `lib/session/db/sessionRepository.ts` (`SegmentRecord`, `getSegmentsForSession`, extended `startSession` interface) → `InMemorySessionRepository`/`SupabaseSessionRepository` (parallel implementations) → `lib/session/startSession.ts` (fast-path `NoCurrentSegmentToContinueError` check, mirroring the existing `previousInteraction` fast-path) → `lib/session/getSession.ts` (`segmentNumber` matched by `currentInteraction.segmentId`, not array position) → `app/api/sessions/[identifier]/start/route.ts` (parses/validates `segmentTarget`, maps the new error to 409) → `public/host.html` (Turn label, next-Turn computation, button retarget + relabel).

## Tests

- `__tests__/segment.test.ts` (11 tests, in-memory): ordinal allocation (1, sequential, 3+), `CURRENT_SEGMENT` composition, concurrent `NEW_SEGMENT` (exactly one succeeds, no duplicate ordinal), `NoCurrentSegmentToContinueError`, `CURRENT_SEGMENT` still enforcing `PreviousInteractionNotRevealedError`, `segmentNumber` vs `interactionNumber` divergence, the full Best Joke sequence, rematch isolation, STRANDED administrative completion.
- `__tests__/segmentSupabaseRepository.contract.test.ts` (7 tests, real Postgres): schema reachability, live ordinal allocation (1→2), live Best Joke (`CURRENT_SEGMENT` reuse), concurrent `NEW_SEGMENT`, concurrent `CURRENT_SEGMENT`, database-level rejection of a duplicate `(session_id, segment_ordinal)`, database-level rejection of cross-session `(session_id, segment_id)` membership — the last two bypass the application layer entirely via direct inserts, since no repository method can construct either invalid state.

All previously-existing engine regression coverage (Multiple Choice, Voting, Open Response, Session Continuity) re-verified passing unchanged.

## Database-Backed Validation (this session)

**Environment**: the prior session's local Dockerized `urbano-gaming` Supabase environment (established during Slice 007 validation) was found already running once Docker Desktop was started — `supabase_*_urbano-gaming` containers on ports 54421 (API)/54422 (DB)/54423 (Studio), migrations 0001–0034 already applied, `supabase/config.toml` confirming `project_id = "urbano-gaming"`. The unrelated `level33-mvp` containers (ports 5432x) were left completely untouched throughout — never stopped, reset, or migrated. Confirmed empirically, before any DB operation, that shell-level `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` overrides take precedence over `.env.local` (which points at production) in both the Vitest env loader and the Next.js dev server — production was never read, written, or touched.

**Migration rehearsal**: augmented the existing local data (2 sessions, 3 Interaction Instances, real Slice 007 leftovers) with deliberately constructed representative pre-Slice-008 history — a Multiple-Choice-only session, a 3-interaction mixed-engine session, and a session with **two Interaction Instances sharing a byte-identical `created_at` timestamp**, specifically to exercise the historical tie-break. Applied `0035` → `0036` → `0037` in order against this real data, then queried and inspected the resulting rows directly (not merely checked exit codes): one Segment per pre-existing Interaction Instance (9-for-9, later 11-for-11 after two more real rows were added by the concurrency proof below); zero null `segment_id`; every Segment's `session_id` matching its Interaction Instance's `session_id` with zero drift; per-session ordinals contiguous from 1 (verified `min=1, max=count` for every session); the empty session correctly produced zero Segments; the tie-break pair resolved deterministically to ordinals 1 and 2 via `interaction_instance_id`; `multiple_choice_details` rows remained correctly attached to their original `interaction_instance_id`, untouched by the migration. After the index defect was found and fixed, performed a full rollback (drop constraint, drop column, drop table) and clean reapply of the corrected three-migration sequence against the same data, re-verifying every one of the above checks a second time.

**Cascade-delete verified directly, not assumed**: created a session with a real Segment + Interaction Instance via the actual RPC, then deleted the session row directly. Confirmed zero orphaned rows and zero FK violations from the new multi-path cascade (`sessions` → `segments` and `sessions` → `interaction_instances`, with `interaction_instances` → `segments` via the new composite FK, no `ON DELETE` clause specified) — Postgres resolves this correctly within one statement regardless of internal cascade ordering. (A separate, pre-existing, unrelated block from `session_events`'s own non-cascading FK was encountered and cleared first — not a Slice 008 concern; no code path in this application ever deletes a session directly.)

**Composite FK / constraint verification**: `\d segments` / `\d interaction_instances` confirmed `segments_session_id_segment_id_unique`, `segments_session_id_segment_ordinal_unique`, and `interaction_instances_session_segment_fkey` all present with the exact intended definitions, plus (after the fix) `interaction_instances_segment_id_idx`.

**Contract tests**: `__tests__/segmentSupabaseRepository.contract.test.ts` — 7/7 passing against local Postgres. Full `test:contract` (16 pre-existing + 18 Voting + 7 Segment) — **41/41 passing**.

## Concurrency Evidence

Two distinct kinds of evidence, not conflated:

1. **Real, independent HTTP requests** (the contract-test suite's "two genuinely concurrent NEW_SEGMENT/CURRENT_SEGMENT calls" tests) — two separate `SupabaseSessionRepository.startSession()` calls fired via `Promise.allSettled`, each a genuinely separate PostgREST HTTP request/connection. Passed: exactly one success, one `PreviousInteractionNotRevealedError`, no duplicate ordinal.
2. **A deterministically engineered proof**, using two raw, separate `psql` connections against the same session, specifically because a probabilistic race is not the same evidence as a proven lock: Connection A opened an explicit transaction, called `start_session_atomically`, then held the session-row lock via `pg_sleep(12)` before committing — real, measured wall-clock timestamps (`clock_timestamp()`) captured at each step. Connection B, a fully independent connection, attempted the same RPC for the same session while A was mid-hold. **B's statement blocked for 5.77 seconds** (measured directly by `psql`'s own `\timing`) and completed within 2.4ms of A's commit — proving the actual `FOR UPDATE` row-lock serialization mechanism directly, not merely its downstream effect. B then correctly re-evaluated fresh (not a stale read) and received `PREVIOUS_INTERACTION_NOT_REVEALED`, matching the design-review's traced prediction exactly.

## Browser Operational Simulation

Ran the real Next.js app against the local Supabase stack (a temporary `.claude/launch.json` entry injecting the local URL/key as process env vars, reverted to byte-identical with its committed state before finishing — `.env.local` never touched) through the actual `host.html`/`participant.html` UI in the Browser pane, with two participants (Alex, Jordan) plus a third (Sam) for the rematch:

- **Turn 1 (Open Response)**: button correctly read "Start Turn 1" before any Segment existed. Two real submissions (chicken joke, knock-knock joke), close, reveal.
- **"Continue this Turn with Voting on those responses"** (the retargeted button): clicked with real prompt text. **Database confirmed the new Voting Interaction Instance shares the exact same `segment_id` as the Open Response one, both `segment_ordinal = 1`.** UI **still read "Turn 1"** throughout.
- Both participants voted (a deliberate 1–1 split), close, reveal: **placement correctly showed a tie — both candidates ranked `#1`, 1 vote each** — standard competition ranking confirmed live.
- **Turn 2 (Multiple Choice, per founder preference to regression-test automatic scoring)**: seeded a prepared question via the same API route the authoring UI calls (the interactive authoring panel proved flaky under this app's fast GET_SESSION poll cycle — a pre-existing UI characteristic unrelated to Slice 008, worked around rather than fought), then drove "Start Turn 2" through the real UI. **Database confirmed `segment_ordinal = 2`** — a genuinely new Segment. UI **correctly read "Turn 2."** Alex answered correctly, Jordan incorrectly; reveal correctly awarded Alex 50 points, Jordan 0 — automatic scoring fully unaffected by the Segment changes.
- **Session completion and rematch**: completed with the correct winner banner (Alex, 50 pts). Created a rematch through the real "Create Rematch" button. **Database confirmed the new session has zero Segments and the predecessor's own 2 Segments remain completely untouched.** Joined Sam, locked, started an ad-hoc Turn: **`segment_ordinal = 1`**, UI read "Turn 1" — the rematch's own ordinal sequence starts fresh, fully independent of the predecessor's.

One UI-automation-specific finding, not a product defect: this app's fast GET_SESSION poll cycle causes DOM element references to go stale between a read and a subsequent click more often than in a slower-polling app; worked around by re-reading immediately before each click or, where refs still raced, clicking by screenshot-pixel coordinate instead. Left no trace in the application itself. A second, separate finding — a back-forward-cache-style staleness where a same-tab `navigate` briefly restored a prior session's in-memory JS state even though `sessionStorage` already held the correct new session — was resolved with a forced reload; also a browser-automation characteristic, not an application defect (confirmed: `sessionStorage` itself always held the correct value).

## `segmentNumber` vs `interactionNumber` — Load-Bearing Invariant, Confirmed

After the Best Joke sequence: Segment count = 1, Interaction Instance count = 2 → `segmentNumber = 1`, `interactionNumber = 2`, UI displayed "Turn 1" — confirmed both in the automated test suite and live in the browser. After the subsequent Multiple Choice Turn: Segment count = 2, Interaction Instance count = 3 → `segmentNumber = 2`, `interactionNumber = 3`, UI displayed "Turn 2" — confirmed the same way. The existing `interactionNumber`-keyed result-cache-invalidation logic in `host.html`/`participant.html` required **zero changes** and was confirmed still firing correctly across the `CURRENT_SEGMENT` transition (Voting's own results did not leak Open Response's stale cache) — because that logic was always correctly scoped to Interaction Instance identity, not Turn identity, a distinction load-bearing to this slice's entire design.

## Future Direction Pressure Test — Virtual Table / Private Hand (recorded, not implemented)

Per founder direction, checked only whether Segment creates an obstacle to a future multi-phase, rotating-role, private-hand table-game direction — no design work performed. Finding: it does not appear to. Segment's only committed responsibility is grouping Interaction Instances under one Turn identity; it carries no assumption about how many participants act within a phase, whether their views differ, or who currently holds authority. A future "rotating dealer/judge" Turn would plausibly still be one Segment containing one or more Interaction Instances, the same shape Best Joke already proves; a private-hand/hidden-role mechanic would plausibly be a property of a future Interaction Instance or a new Private Player State concept sitting beside Segment, not one Segment needing to know about multiple simultaneous private views of itself. This is a shallow compatibility check, not a design — Private Player State, Shared Game State, and any Virtual Card Deck concept remain fully undesigned and unimplemented, and this note should not be read as pre-deciding any of them.

## Old-Application / New-Schema Backward-Compatibility Evidence

Because Vercel is now GitHub-integrated (a push to `main` triggers automatic deployment) and this slice has database dependencies, the currently-deployed pre-Slice-008 application must keep working correctly during the window between applying 0035–0037 to production and deploying Slice 008's own code. Verified, not assumed:

- **The deployed (HEAD) app's only write path to `interaction_instances` is `start_session_atomically`** — confirmed by inspecting `git show HEAD:lib/session/db/supabaseSessionRepository.ts` directly: its sole non-RPC `interaction_instances` access is a `select("*")` in `getInteractionInstancesForSession`, mapped via explicit named fields (`row.session_id`, `row.state`, ...) that silently ignore any column it doesn't name — an added `segment_id` column is invisible to it. `close_submissions_atomically`/`reveal_results_atomically` only `UPDATE state`/`updated_at` on already-existing rows; neither inserts, so `segment_id NOT NULL` cannot break them.
- **0037's new `start_session_atomically` always sets `segment_id` on every `INSERT INTO interaction_instances`, unconditionally** — verified by re-reading all three insert branches (prepared-question, voting, plain) — so no code path, old or new, can ever produce a null-`segment_id` row.
- **Empirically replayed the deployed app's exact old 7-parameter RPC request shape** (the literal JSON body `git show HEAD` sends — no `p_segment_target` key at all) via raw HTTP against the post-0037 local database, for all three engine paths:
  - Old-shape Open Response start → succeeded, `segment_ordinal: 1`, underlying row got a valid non-null `segment_id` automatically.
  - Old-shape Multiple Choice start (via `p_prepared_question_id`) → succeeded, `segment_ordinal: 2`.
  - Old-shape standalone host-authored Voting start → succeeded, `segment_ordinal: 3`.
  - Old-shape `close_submissions_atomically`/`reveal_results_atomically` calls (unchanged signatures) → succeeded, confirming those RPCs are untouched by this slice.
- **`p_segment_target text default 'NEW_SEGMENT'`** is a real SQL-level default — PostgREST/Postgres apply it automatically when a named parameter is omitted from the RPC JSON body, exactly as observed above.
- **No overload ambiguity possible**: queried `pg_proc` directly after migration — exactly one `start_session_atomically` signature exists post-0037 (the new 8-arg one; 0037's own `drop function if exists ... (uuid,text,text,uuid,text,jsonb,uuid)` removes the old 7-arg signature first). There is no old signature left for PostgREST to resolve against ambiguously.

**Conclusion: OLD deployed application code + NEW post-0037 production schema remains fully functional**, for all three engine paths, proven empirically against a real Postgres instance using the literal old request shape — not inferred from reading the migration SQL alone.

## Production Migration Preflight (read-only, this session)

Performed against the linked production project (`URBANO Gaming`, ref `uyxckhbmcbctsbewnqkb`) via `supabase migration list --linked` and `supabase db query --linked` restricted to `SELECT` statements throughout. No write, no DDL, no `supabase db push`.

- **Migration history**: local and remote agree exactly through `0034`; `0035`/`0036`/`0037` show an empty remote field — confirmed not yet applied.
- **No `segments` table** (`to_regclass('public.segments')` → `null`).
- **No `interaction_instances.segment_id` column** (empty result from `information_schema.columns`).
- **Current `start_session_atomically` signature**: exactly the expected pre-0037 7-argument version, byte-for-byte matching 0033's definition — no drift.
- **No orphaned Segment-related objects**: `pg_constraint` search for `%segment%` returned zero rows — no partial artifact from any prior attempt.
- **Data volume**: 96 sessions, 72 Interaction Instances total — small. Max 6 Interaction Instances in a single session, average 1.76 per session. **Zero sessions have any duplicate `created_at` timestamp** — the historical tie-break logic in 0036 will not even be exercised by real production data, though it remains in place as a safety net.
- **RLS/grant implications for the new `segments` table**: confirmed the `ensure_rls` event trigger (found and documented during Slice 007's own production validation) is still present and enabled (`evtenabled: O`) on `ddl_command_end`, and `service_role` still has `rolbypassrls = true`. This means `segments`, once created by 0035, will automatically receive the identical deny-by-default RLS posture every other table in this schema already has — no policy needed, no migration change needed, the same mechanism that already covers `voting_candidates`/`votes` since Slice 007 applies uniformly to any new table in `public`.

**No blocker found.** Production is a clean, expected pre-Slice-008 state with no drift, small enough backfill volume to be low-risk, and the identical RLS/grant automation Slice 007 already proved correct will apply to the new table without any additional action.

## Deferred Items

Turn Type selector; PARTICIPANTS Voting source; Voting scoring; Prediction/Golazo; canonical URBANO auth; geolocation; persistent Leaderboards backend; Rewards backend; Level 33; Shared Game State; Teams; generic orchestration; new Engines; virtual-table/private-hand implementation. A genuine multi-party concurrent-load playtest against production (this slice's local validation covered correctness under engineered and real-request concurrency, not production request volume).

## Production Status

**Not applied.** No migration has been run against production. No push, no deploy. `.claude/launch.json` reverted to its exact committed state. All local Docker validation used the pre-existing `urbano-gaming` local environment; `level33-mvp` was never touched. The implementation candidate is accepted; production migration and deployment remain a separate, later, explicitly-authorized checkpoint per the updated GitHub-integrated Vercel deployment procedure (push to `main` now triggers automatic deployment, so migrations 0035–0037 must be applied to production *before* that push, not after).
